{
  "name": "@vura/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types for the Vura monorepo",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
